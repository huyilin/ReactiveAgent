package template;
import logist.topology.Topology.City;

public class pdAction {
	City nextCity;
	String key;
	public pdAction(int nextCity) {
		this.iftake = ifTake;
		this.nextCity = nextCity;
		this.key = Integer.toString(iftake) + ',' + Integer.toString(nextCity.id);
	}
}